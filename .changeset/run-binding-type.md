---
"@evolu/react": major
---

Changed `createRunBinding` and `createEvoluBinding` to accept type arguments instead of unused runtime arguments. `createRunBinding` now accepts a `DisposableRun` type while exposing `Run` to React components.
