{
  "name": "server",
  "version": "1.0.58",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsc && node dist/index.js",
    "build": "rm -rf dist && tsc",
    "start": "node dist/index.js",
    "lint": "eslint src --ext .ts,.tsx",
    "clean": "rm -rf .turbo node_modules dist db.sqlite"
  },
  "dependencies": {
    "@evolu/server": "workspace:*"
  },
  "devDependencies": {
    "@evolu/tsconfig": "workspace:*",
    "@types/node": "^20.12.12",
    "ts-node": "10.9.2",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=16.15"
  }
}